import { Button } from "antd";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "صحه اصلی",
};

export default function Dashboard() {
  return <Button type="primary">منوما</Button>;
}
